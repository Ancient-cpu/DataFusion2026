# 🧙‍♂️ Data Fusion Contest 2026: Задача "Герои" (VRPTW)
### Ультимативное ML-решение (God-Mode Ensemble + RL Rollouts + Optuna)

![Python](https://img.shields.io/badge/python-3.9+-blue.svg)
![Optuna](https://img.shields.io/badge/Optuna-Meta_Tuning-EF5136.svg)
![CatBoost](https://img.shields.io/badge/CatBoost-Ensemble-yellow.svg)
![LightGBM](https://img.shields.io/badge/LightGBM-Ensemble-blue.svg)
![XGBoost](https://img.shields.io/badge/XGBoost-Ensemble-red.svg)
![NetworkX](https://img.shields.io/badge/NetworkX-Graph_Features-lightgrey.svg)

Решение для образовательного трека **[Data Fusion Contest 2026](https://ods.ai/tracks/data-fusion-2026-competitions)** (Спецноминация Companion). Задача представляет собой нестандартную вариацию **Vehicle Routing Problem with Time Windows (VRPTW)**, стилизованную под культовую игру "Герои Меча и Магии".

---

## 🎯 Описание задачи

Вам предстоит управлять неограниченным пулом Героев (курьеров), чтобы собрать максимальное количество золота с Водяных Мельниц (заказов) за одну игровую неделю (7 дней). 

**Ключевые усложнения:**
- **Гетерогенный флот:** У каждого героя свой уникальный запас очков хода (`move_points`).
- **Временные окна:** Каждая мельница открывается в строго определенный день (`day_open`).
- **Строгая очередь найма:** Герои нанимаются строго по порядку (правило "Таверны").
- **Правило Last Move:** Если у героя осталось от 1 до 100 очков, он может посетить мельницу (стоимостью 100 очков) "бесплатно" на последнем издыхании. Это важнейшая игровая механика!
- **Экономика:** Мельница приносит `+500` золота, найм героя стоит `-2500` золота. Цель — максимизировать итоговую прибыль.

---

## 🧠 Архитектура решения (God-Mode Ensemble)

Данный код отходит от классических жадных алгоритмов и использует продвинутую связку **Обучения с подкреплением (RL)**, **Теории Графов** и **Ансамблирования ML-моделей** под управлением мета-оптимизатора.

Решение разбито на 5 фаз:

### 🔹 Фаза 1: Graph Feature Engineering
Матрица расстояний преобразуется в граф связности с помощью `NetworkX`. Извлекаются сложные пространственные признаки:
- `PageRank` — определяет "центры притяжения" мельниц.
- `Closeness Centrality` — насколько быстро из данной точки можно добраться до любой другой.
- `Distance to Depot` — расстояние до стартового замка.

### 🔹 Фаза 2: Сбор RL-опыта (Monte-Carlo Rollouts)
Вместо того чтобы обучать модель на готовых (чужих) решениях, алгоритм генерирует опыт сам. Совершается несколько проходов симуляции недели со случайным шумом (Exploration). Для каждого шага записывается вектор признаков и целевая переменная — **кумулятивная награда до конца жизни героя**.

### 🔹 Фаза 3: Обучение Гетерогенного Ансамбля
На собранном опыте обучаются 5 моделей различной математической природы (для снижения Variance и защиты от переобучения):
1. `CatBoostRegressor`
2. `XGBoost`
3. `LightGBM`
4. `AdaBoost`
5. `BayesianRidge` (Байесовская регрессия)

### 🔹 Фаза 4: Optuna Meta-Tuning 
`Optuna` не просто подбирает гиперпараметры моделей. Она подбирает **идеальные веса для блендинга** предсказаний 5 моделей и **веса для эвристик** (бонус за Last Move, бонус за ROI, бонус за PageRank, штраф за удаление от базы). 

### 🔹 Фаза 5: Economic Pruning (Умный Инференс)
Генерируется финальный маршрут. В конце алгоритм ретроспективно анализирует результативность каждого героя. Если последние нанятые герои суммарно принесли меньше золота (напр., 1500), чем стоил их найм (2500), они **автоматически "увольняются"** из итогового сабмита для максимизации `VRPTW Score`.
