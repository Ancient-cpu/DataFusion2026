# 🧙‍♂️ Data Fusion Contest 2026: Задача "Герои" (VRPTW)
### Комплексное ML-решение (Ensemble + MC Rollouts + Optuna + Graph FE)

![Score](https://img.shields.io/badge/VRPTW_Score-282_000-ffd700?style=for-the-badge&logo=kaggle)
![Python](https://img.shields.io/badge/python-3.9+-blue.svg?style=for-the-badge&logo=python)
![Optuna](https://img.shields.io/badge/Optuna-Meta_Tuning-EF5136.svg?style=for-the-badge)
![CatBoost](https://img.shields.io/badge/CatBoost-Ensemble-yellow.svg?style=for-the-badge)
![LightGBM](https://img.shields.io/badge/LightGBM-Ensemble-blue.svg?style=for-the-badge)

Решение для образовательного трека **[Data Fusion Contest 2026](https://ods.ai/tracks/data-fusion-2026-competitions)** (спецноминация Companion). Задача представляет собой нестандартную вариацию **Vehicle Routing Problem with Time Windows (VRPTW)**, стилизованную под механику культовой игры «Герои Меча и Магии».

---

## 🏆 Результаты

На данный момент алгоритм достигает выдающегося показателя:
### **VRPTW Score: 282 000** 🥇

Этот результат достигается за счет баланса между количеством собранных ресурсов (мельниц) и минимизацией затрат на логистику и наём новых героев, что подтверждает высокую эффективность разработанного ансамбля моделей и эвристик.

---

## 🎯 Описание задачи

Необходимо управлять пулом из 100 доступных героев (курьеров), чтобы собрать максимальное количество золота с 700 Водяных Мельниц (заказов) за одну игровую неделю (7 дней). 

**Ключевые усложнения и механики:**
- **Гетерогенный флот:** У каждого героя свой уникальный запас очков хода (`move_points`).
- **Жёсткие временные окна:** Каждая мельница открывается в строго определенный день (`day_open`). Раннее прибытие сжигает очки хода на ожидание, позднее — лишает награды.
- **Очередь найма (правило «Таверны»):** Герои нанимаются строго последовательно. Чтобы использовать 5-го героя, придется оплатить найм предыдущих четырех.
- **Механика Last Move («на-ласт-мув»):** Если у героя осталось от 1 до 100 очков, он может посетить мельницу (базовая стоимость 100 очков) на остатке сил. Успешная эксплуатация этого правила дает огромный прирост к экономике.

---

## 🧠 Архитектура решения

Данный код отходит от классических жадных алгоритмов. Пайплайн объединяет **генерацию опыта (Monte-Carlo Rollouts)**, **графовые вычисления** и **ансамблирование разнородных ML-моделей** под управлением мета-оптимизатора.

Решение разбито на 5 логических фаз:

### 🔹 Фаза 1: Graph Feature Engineering
Матрица расстояний конвертируется во взвешенный граф с помощью `NetworkX`. Извлекаются сложные пространственные признаки:
- `PageRank` — определяет топологические «центры притяжения» мельниц.
- `Closeness Centrality` — оценивает, насколько быстро из данной точки можно добраться до любой другой.
- `Distance to Depot` — штрафной признак удаленности от стартового замка.

### 🔹 Фаза 2: Сбор опыта (Monte-Carlo Rollouts)
Вместо обучения на сторонних решениях алгоритм генерирует датасет самостоятельно. Совершаются множественные симуляции игровой недели с добавлением случайного шума (Exploration). Для каждого шага фиксируется вектор признаков, а **целевой переменной (Target)** выступает кумулятивная награда (MC return), собранная героем до конца его пути.

### 🔹 Фаза 3: Обучение гетерогенного ансамбля
Для обеспечения разнообразия (diversity) и защиты от переобучения (overfitting) на собранном опыте обучаются 5 моделей различной математической природы:
1. `CatBoostRegressor` (Симметричные деревья)
2. `XGBoost` (Классический градиентный бустинг)
3. `LightGBM` (Построение деревьев по листьям)
4. `AdaBoostRegressor` (Адаптивный бустинг)
5. `BayesianRidge` (Байесовская линейная регрессия)

### 🔹 Фаза 4: Optuna Meta-Tuning 
Фреймворк `Optuna` используется не для банального поиска гиперпараметров деревьев, а для **мета-оптимизации инференса**. Он подбирает идеальные веса для блендинга предсказаний всех 5 моделей, а также калибрует веса бизнес-эвристик (премии за Last Move, ROI, PageRank и штраф за уход на край карты).

### 🔹 Фаза 5: Economic Pruning (Умный Инференс)
Генерируется финальный маршрут. На последнем этапе алгоритм выполняет ретроспективный экономический анализ: если последние нанятые герои суммарно принесли меньше золота, чем стоил их найм (2500 золота за героя), они **автоматически удаляются** из итогового сабмита для максимизации итогового `VRPTW Score`.
